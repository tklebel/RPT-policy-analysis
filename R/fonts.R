# for the figurto compile correctly, you need to install the font `Hind` on your
# system. After that, the following line only needs to be run once
# extrafont::font_import()

# customise the following to your device

suppressMessages(extrafont::loadfonts(device = "win"))
